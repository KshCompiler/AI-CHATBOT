[project]
name = "chatbot"
version = "0.1.0"
description = "this is a normal ai chatbot"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.14",
    "python-dotenv",
    "langchain-groq",
    "langgraph>=1.2.9",
    "langsmith>=0.10.5",
]
